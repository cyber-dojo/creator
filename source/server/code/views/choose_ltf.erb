<script>
'use strict';
$(() => {

  const urlParams = () => {
    const s = window.location.search;
    return s.substring(s.indexOf('?') + 1);
  };

  $('button.next').click(() => {
    const ltfName = encodeURIComponent(cd.selectedDisplayName);
    window.location = `/creator/choose_type?${urlParams()}&ltf_name=${ltfName}`;
  });

});
</script>

<div class="choose-ltf-page">
  <div class="edged-panel">
    <div class="title">create a new exercise</div>
    <div class="sub-title">step 2: choose a language &amp; test-framework</div>
    <%= erb :'shared/display_names' %>
    <button type="button" class="next">next</button>
  </div>
<div>
