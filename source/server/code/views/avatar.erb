<script>
'use strict';
$(() => {

  const kataId = "<%= @kata_id %>";

  $.getJSON('/images/avatars/names.json', {}, (avatarNames) => {
    
  });

  $('button.ok').click(() => {
    window.location = '/creator/home';
    window.open(`/kata/edit/${kataId}`);
  });

});
</script>

<div id="avatar-page">
  <div class="edged-panel">
    <div class="title">your avatar is</div>
    <table>
      <% 8.times do %><tr>
        <% 8.times do %><td>
          <img class="small colour avatar"
               alt="<%= @avatar_name %>"
               src="/images/avatars/<%= @index %>.jpg"/>
        </td><% end %>
      </tr><% end %>
    </table>
    <div class="avatar-name">the <%= @avatar_name %></div>
    <button type="button" class="large ok">ok</button>
  </div>
</div>
