<script>
'use strict';
$(() => {

  $('button.create').click(() => {
    const displayName = cd.selectedDisplayName;
    const param = 'display_name=' + encodeURIComponent(displayName);
    window.location = `/creator/kata_custom_create?${param}`;
  });

});
</script>

<div class="chooser-page">
  <div class="chooser edged-panel">
    <%= erb :'shared/custom_heading' %>
    <%= erb :'shared/display_names' %>
    <button type="button" class="create">create the exercise</button>
  </div>
<div>
