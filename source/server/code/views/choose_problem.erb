<script>
'use strict';
$(() => {

  $('button.next').click(() => {
    const name = cd.selectedDisplayName;
    const params = `exercise_name=${encodeURIComponent(name)}`;
    cd.goto(`/creator/choose_ltf?${cd.urlParams()}&${params}`);
  });

  $('button.skip').click(() => cd.goto(`/creator/choose_ltf?${cd.urlParams()}`));

  $('button.switch').click(() => cd.goto(`/creator/choose_custom_problem?${cd.urlParams()}`));

});
</script>

<div class="choose-problem-page">
  <div class="edged-panel">
    <div class="title">create a new exercise</div>
    <div class="sub-title">choose a problem</div>
    <%= erb :'shared/display_names' %>
    <button type="button" class="next">next</button>
    <button type="button" class="skip">skip</button>
    <button type="button" class="switch">switch to custom problems</button>
  </div>
<div>
